# Secrux Engines build configuration
# Copy to `.env` and adjust for your environment.

# Base upstream images
SEMGREP_IMAGE=semgrep/semgrep:latest
TRIVY_IMAGE=aquasec/trivy:latest

# Output image tags
SECRUX_SEMGREP_ENGINE_IMAGE=secrux-semgrep-engine:latest
SECRUX_TRIVY_ENGINE_IMAGE=secrux-trivy-engine:latest

